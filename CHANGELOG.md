# บันทึกการเปลี่ยนแปลง

รูปแบบเวอร์ชันตาม [Semantic Versioning](https://semver.org/lang/th/) — `MAJOR.MINOR.PATCH`

- **MINOR** (`1.30.0` → `1.31.0`) เพิ่มความสามารถใหม่
- **PATCH** (`1.30.1` → `1.30.2`) แก้ข้อบกพร่องหรือปรับปรุงย่อย

---

## 1.48.0 — 2026-09-16

**เพิ่ม**
- **แฟ้มภาพเล็กใต้จอฉาย** — เห็นทั้งหมดในกล่องเดียวว่ามีโปสเตอร์อะไรอีกบ้าง ใบที่กำลังฉายมีกรอบสีธีมและยกตัวขึ้น ใบอื่นจางลง กดข้ามไปใบที่สนใจได้เลยไม่ต้องรอวน (เดิมมีแค่จุดเล็กๆ ซึ่งบอกได้แค่จำนวน ไม่บอกว่าเป็นภาพอะไร)
- แฟ้มภาพเลื่อนตามให้ใบที่กำลังฉายอยู่ในสายตาเสมอเมื่อมีโปสเตอร์เยอะ

**แก้ไข**
- **คำบรรยายเคยทับตัวโปสเตอร์** — โปสเตอร์ราชการมักมีแถบชื่อหน่วยงานและช่องทางติดต่ออยู่ขอบล่างของภาพเอง พอเอาคำบรรยายไปทับก็บังข้อมูลที่หน่วยงานตั้งใจใส่มา ย้ายคำบรรยายลงมาไว้ใต้จอฉายแทน ตรวจด้วยการวัดพิกัดจริงแล้วว่าไม่ทับกันแม้แต่พิกเซลเดียว
- คำบรรยายทุกใบซ้อนอยู่ตำแหน่งเดียวกันและกันความสูงไว้คงที่ เปลี่ยนภาพแล้วเนื้อหาใต้กล่องจึงไม่กระตุกขึ้นลง

---

## 1.47.0 — 2026-09-16

**เพิ่ม**
- **โหมด "โรงฉาย" สำหรับนิทรรศการโปสเตอร์** — ให้ความรู้สึกเหมือนวิดีโอพรีเซนต์ ไม่ใช่แกลเลอรีที่เลื่อนได้เฉยๆ และตั้งเป็นค่าเริ่มต้นของ section นี้
  - **ฉากหลังเบลอจากภาพใบเดียวกัน** — โปสเตอร์แนวตั้งบนเวทีกว้างย่อมเหลือที่ว่างสองข้างเสมอ ถ้าปล่อยเป็นพื้นขาวจะดูเหมือนภาพหลุดมาวาง แต่พอเอาภาพเดิมมาเบลอเป็นฉากหลัง สีจะกลมกลืนกับตัวโปสเตอร์เอง (วิธีเดียวกับที่แอปดูหนังและแอปฟังเพลงใช้)
  - **ซูมช้าตลอดเวลาที่ภาพค้างอยู่ (Ken Burns)** — ภาพนิ่งจึงดูมีชีวิตเหมือนกล้องค่อยๆ เคลื่อน ระยะเวลาซูมเท่ากับจังหวะเปลี่ยนภาพที่ตั้งไว้พอดี
  - **เปลี่ยนภาพด้วยการค่อยๆ จางทับกัน** ไม่ใช่กระโดด พร้อมคำบรรยายลอยขึ้นมาบนแถบไล่สี
  - **แถบเวลาบอกว่าอีกนานแค่ไหนจะเปลี่ยนภาพ** และปุ่มเลื่อนลอยอยู่บนเวที โผล่เมื่อเอาเมาส์เข้าใกล้
  - โปสเตอร์ยังไม่ถูกครอปเหมือนเดิม ตรวจแล้วว่าสัดส่วนที่แสดงตรงกับไฟล์ต้นฉบับ

**ปรับปรุง**
- การเคลื่อนไหวทั้งหมดของโหมดนี้ทำด้วย CSS animation ไม่ใช่ JavaScript — ลื่นระดับการ์ดจอ ไม่กินซีพียู และไม่ต้องรอ `requestAnimationFrame`
- เอาเมาส์ไปวางแล้วหยุดค้างทั้งการนับเวลา การซูม และแถบเวลา (เดิมหยุดแค่การนับ แถบเวลาจะวิ่งจนเต็มแล้วค้าง ดูเหมือนระบบค้าง)
- โหมดสลับใบ (โรงฉาย / จางสลับ) วนกลับมาใบแรกได้ ปุ่มย้อนกลับจึงไม่ถูกปิดตอนอยู่ใบแรกอีกต่อไป

---

## 1.46.0 — 2026-09-16

**เพิ่ม**
- **section ใหม่ "นิทรรศการโปสเตอร์" บนหน้าแรก** — จัดแสดงภาพประชาสัมพันธ์และอินโฟกราฟิกเป็นสไลด์ รองรับทั้งแนวตั้ง แนวนอน จัตุรัส และแนวยาวพิเศษ **คละกันในแถวเดียวได้โดยไม่ครอปภาพ**
  - วิธีทำ: ตรึงความสูงเวทีไว้เท่ากันทุกใบ แล้วให้ความกว้างผันไปตามสัดส่วนของภาพเอง ทุกใบจึงวางบนเส้นฐานเดียวกัน (แนวเดียวกับที่หน้าสินค้าของ Apple ใช้จัดภาพหลายสัดส่วน)
  - **ไม่ครอปเพราะโปสเตอร์ราชการมีข้อความอยู่ทุกมุม** ครอปเมื่อไหร่คือตัดเนื้อหาทิ้ง
  - บันทึกขนาดจริงของภาพตอนอัปโหลด แล้วใช้จองพื้นที่ล่วงหน้า แถวจึงไม่กระโดดตอนภาพทยอยโหลด
- **หน้า admin "นิทรรศการโปสเตอร์"** — เพิ่ม/แก้ไข/ลบ/ลากจัดลำดับ ผูกลิงก์ปลายทางรายใบได้ ลบแล้วกู้จากถังขยะได้
- **ลูกเล่นตั้งได้จาก admin** — รูปแบบการเล่น 3 แบบ (ไล่ระดับเวทีกลาง / แถบเลื่อนต่อเนื่อง / จางสลับทีละใบ) × ความสูง 3 ระดับ + เลื่อนอัตโนมัติพร้อมตั้งจังหวะ + เปิดปิดคำบรรยาย กรอบเงา และการคลิกดูขนาดเต็ม
- คลิกภาพเพื่อดูขนาดเต็มจอ ปิดด้วยปุ่ม คลิกพื้นหลัง หรือปุ่ม Esc — จำเป็นกับโปสเตอร์แนวตั้งที่มีตัวหนังสือเยอะ ซึ่งอ่านจากขนาดย่อไม่ออก

**ปรับปรุง**
- เลื่อนอัตโนมัติจะหยุดเองเมื่อผู้ชมเอาเมาส์ไปวาง กำลังใช้คีย์บอร์ดอยู่ในกล่อง หรือสลับแท็บไปทำอย่างอื่น
- เคารพการตั้งค่าลดการเคลื่อนไหวทั้งของเว็บ (anim-off) และของเครื่องผู้ใช้ (prefers-reduced-motion) — ปิดทั้งการเลื่อนอัตโนมัติและอนิเมชัน
- การเลื่อนเขียนเป็นอนิเมชันของระบบเองแทนการพึ่ง `scroll-behavior: smooth` ของเบราว์เซอร์ เพราะบางเบราว์เซอร์ยกเลิกอนิเมชันทิ้งเมื่อใช้คู่กับ scroll snap แล้วเด้งกลับที่เดิม และมีทางลงสำรองเสมอสำหรับเบราว์เซอร์ฝังที่ไม่เรียก `requestAnimationFrame` เลย (ยอมให้ไม่นุ่ม ดีกว่ากดปุ่มแล้วไม่เกิดอะไรขึ้น)

---

## 1.45.0 — 2026-09-15

**เพิ่ม**
- **หน้าตั้งค่า 2FA มี QR Code ให้สแกนแล้ว** — เดิมต้องพิมพ์คีย์ 32 ตัวเข้าแอปเอง ซึ่งพิมพ์ผิดง่ายและเสียเวลา ตอนนี้เปิดแอป Authenticator แล้วส่องได้เลย ยังคงแสดงคีย์ให้พิมพ์เองไว้เป็นทางเลือกสำรองกรณีกล้องใช้ไม่ได้
- เพิ่ม `includes/qrcode.php` — ตัวสร้าง QR เขียนเองทั้งหมด ไม่ต้องลง Composer ไม่ต้องติดตั้งส่วนขยาย PHP เพิ่ม รองรับโหมด Byte ระดับกันพลาด L เวอร์ชัน 1–10 (สูงสุด 271 ไบต์) วาดออกมาเป็น SVG จึงคมทุกความละเอียดและไม่ต้องเขียนไฟล์ลงดิสก์

**ความปลอดภัย**
- **QR สร้างในเครื่องเท่านั้น ไม่พึ่งบริการภายนอก** — QR ของ 2FA บรรจุกุญแจลับของบัญชีผู้ดูแลอยู่ข้างใน ถ้าส่ง URI ไปให้เว็บสร้าง QR ภายนอกวาดให้ (เช่น Google Chart API ที่ตัวอย่างในอินเทอร์เน็ตมักใช้กัน) เท่ากับมอบสิทธิ์เข้าระบบให้เจ้าของบริการนั้นไปด้วย เป็นเหตุผลหลักที่ต้องเขียนตัวสร้าง QR เอง ไม่ใช่เรื่องความสวยงาม
- ชื่อหน่วยงานภาษาไทยยาวๆ ทำให้ URI เกินความจุ QR ได้ (1 ตัวอักษรไทย = 9 ตัวอักษรหลัง percent-encode) ระบบจะไล่ลดรูป URI ลงเองจนพอดี โดยตัดเฉพาะส่วนที่เป็นป้ายชื่อ — กุญแจและชื่อบัญชียังครบถ้วนเสมอ

---

## 1.44.1 — 2026-09-15

**ปรับปรุง**
- **ขยับตราหน่วยงานบนภาพประกาศขึ้น ให้ห่างจากข้อความมากขึ้น** — เดิมตราอยู่ชิดบรรทัดหัวเรื่องย่อยจนอ่านรวมเป็นก้อนเดียวกัน วัดจากภาพที่วาดจริงได้ระยะห่าง 118px บนภาพสูง 1620px ปรับเป็น 156px (ตราขยับขึ้น 19px ข้อความขยับลง 19px เพราะบล็อกทั้งก้อนจัดกึ่งกลางใหม่)
- มีผลกับเทมเพลตที่มีตราทั้งหมด (การ์ดประกาศ / ข้อความเน้น / รูปเต็มพื้น) ให้ระยะเท่ากันทุกแบบ ตรวจแล้วว่าไม่ล้นขอบทั้ง 6 กรณี (3 เทมเพลต × 2 ขนาดภาพ) แม้ใส่หัวเรื่องและเนื้อหายาวเกินปกติ

---

## 1.44.0 — 2026-09-15

**ปรับปรุง**
- **หน้าหลังบ้านซ่อน .php ได้แล้วเหมือนหน้าสาธารณะ** — `/admin/posts` แทน `/admin/posts.php`, `/admin/` แทน `/admin/index.php` ใช้สวิตช์ "URL แบบซ่อน .php" ตัวเดิมที่หน้าตั้งค่า ไม่มีสวิตช์ใหม่ให้ต้องตั้งเพิ่ม
- ลิงก์เดิมที่มี `.php` ยังใช้ได้ ระบบพาไปหน้าใหม่ให้เอง (301) เฉพาะคำขอแบบ GET/HEAD เท่านั้น การส่งฟอร์มจึงไม่มีทางเสียข้อมูลกลางทาง

**ความปลอดภัย**
- **ปิดช่อง `/config` ที่ชี้ไปถึง `config.php` ได้** — URL ที่ไม่มี `.php` ทำให้กฎ `FilesMatch` ที่ห้ามเข้าถึง `config.php` ไม่จับ แล้วกฎ rewrite ก็เติม `.php` ให้ ของจริงไฟล์นี้ถูก execute เฉยๆ ไม่พ่นรหัสผ่านออกมา แต่ไม่ควรฝากความปลอดภัยไว้กับลำดับการทำงานระหว่าง mod_rewrite กับ FilesMatch จึงเพิ่มเงื่อนไขห้าม rewrite ไปยังชื่อนี้ตรงๆ (ช่องนี้มีมาก่อนหน้านี้แล้ว ไม่ได้เกิดจากการเปิดใช้กับหลังบ้าน)
- ไฟล์ที่ขึ้นต้นด้วย `_` (พาร์เชียลอย่าง `_top.php` `_auth.php`) ถูกกันออกจากกฎ URL สวยทั้งฝั่ง Apache และฝั่ง PHP — พาร์เชียลไม่ใช่หน้าที่มี URL ของตัวเอง
- ใส่การ์ด `exit('Forbidden')` ให้ `_brandpanel.php` `_brandmini.php` และ `includes/mark.php` ที่ยังไม่มี

---

## 1.43.1 — 2026-09-15

**แก้ไข**
- **เมนู "เมนูท้ายเว็บ" ขึ้นคำว่า FOOTER เป็นตัวอักษรแทนรูปไอคอน** — ชุดไอคอน Material ไม่มีไอคอนชื่อ `footer` เมื่อใส่ชื่อที่ไม่มีอยู่จริง ฟอนต์จะพิมพ์ชื่อนั้นออกมาเป็นข้อความ เปลี่ยนเป็น `splitscreen_bottom` (กรอบหน้าเว็บที่แถบล่างทึบ) ซึ่งสื่อความหมายตรงและอ่านออกที่ขนาด 18px ในเมนูซ้าย
- ตรวจชื่อไอคอนที่เขียนตายตัวทั้งระบบครบ 165 ชื่อแล้ว มีเพียง `footer` ที่ผิด นอกนั้นถูกต้องทั้งหมด

---

## 1.43.0 — 2026-09-15

**ปรับปรุง**
- **สลับลำดับความสำคัญของแผงซ้ายหน้าเข้าสู่ระบบ** — ตรา AliceCMS ขึ้นไปอยู่บนสุดเป็นตัวเด่น (ตรา 84px + ชื่อระบบ 42px + ป้ายเวอร์ชัน + คำอธิบาย + แถวชิปคุณสมบัติ) ส่วนชื่อหน่วยงานย้ายลงไปท้ายแผงใต้หัวข้อ "ใช้งานโดย" ด้วยขนาดเล็กลง เพราะหน้านี้เป็นหน้าของตัวระบบ หน่วยงานเป็นผู้ใช้งาน ไม่ใช่เจ้าของแบรนด์
- **เอาตราหน่วยงานออกจากหน้าเข้าสู่ระบบ** ทั้งแผงซ้ายและหัวย่อบนจอเล็ก — หน้าเว็บสาธารณะยังใช้ตราหน่วยงานตามเดิมทุกจุด ตัดเฉพาะหน้าหลังบ้าน
- หัวแบรนด์ย่อบนจอเล็กเปลี่ยนเป็นตรา AliceCMS + ชื่อระบบ โดยยังคงแสดงชื่อหน่วยงานเป็นบรรทัดรอง ให้ยืนยันได้ว่าเข้าถูกเว็บ

**ภายใน**
- แยกแผงซ้ายเป็น `admin/_brandpanel.php` และหัวย่อเป็น `admin/_brandmini.php` ใช้ร่วมกันทั้งสามหน้า (เข้าสู่ระบบ / ลืมรหัสผ่าน / ตั้งรหัสผ่านใหม่) แทน `_brandfoot.php` เดิม
- ลบตัวแปร `$logo` `$logoIcon` `$markInner` ที่ไม่ได้ใช้แล้วออกจากทั้งสามหน้า

---

## 1.42.1 — 2026-09-15

**ปรับปรุง**
- **ท้ายแผงแบรนด์หน้าเข้าสู่ระบบทำใหม่ให้ดูเป็นผลิตภัณฑ์จริง** — เดิมเป็นไอคอนจิ๋ว 22px หน้าข้อความสีเทาสองบรรทัด ตอนนี้เป็นล็อกอัปเต็มรูป: ตรา AliceCMS 48px สีธีม + ชื่อระบบตัวหนา + ป้ายเวอร์ชัน + คำอธิบาย "ระบบจัดการเนื้อหาเว็บไซต์สำหรับหน่วยงานราชการ" และแถวชิปบอกคุณสมบัติ (ซอฟต์แวร์โอเพนซอร์ส · ยืนยันตัวตนสองชั้น · บันทึกประวัติการใช้งาน) มีเส้นคั่นด้านบนแยกให้ชัดว่าคนละส่วนกับตราหน่วยงาน
- แถวคุณสมบัติทำเป็นชิปแทนการคั่นด้วยจุด เพราะถ้าคั่นด้วยจุดแล้วแถวตัดบรรทัดจะเหลือจุดลอยอยู่หน้าบรรทัดใหม่ — ชิปตัดบรรทัดได้สวยทุกความกว้าง

**ภายใน**
- ย้ายลายตรา AliceCMS ไปไว้ที่ `includes/mark.php` ที่เดียว แล้วให้แถบบนระบบจัดการ หน้าติดตั้ง และหน้าเข้าสู่ระบบเรียกใช้ร่วมกัน — เดิมเขียนซ้ำสามที่ ถ้าแก้ไม่ครบตราจะเพี้ยนไม่ตรงกัน
- ย้ายบล็อกท้ายแผงแบรนด์ไปไว้ที่ `admin/_brandfoot.php` ใช้ร่วมกันทั้งหน้าเข้าสู่ระบบ ลืมรหัสผ่าน และตั้งรหัสผ่านใหม่ ด้วยเหตุผลเดียวกัน

---

## 1.42.0 — 2026-09-14

**ปรับปรุง**
- **ออกแบบตรา AliceCMS ใหม่ให้เป็นตระกูลเดียวกับเว็บในเครือ** (alicentia.me / alicelium.me) — สามขีดจัดกึ่งกลางกล่องพอดี ไม่เอียงไปทางซ้ายแบบเดิม และย้ายประกาย AI สามดวงไปเป็นเครื่องหมายมุมขวาบน แยกออกจากตัวสัญลักษณ์หลัก
- ตราวาดเป็น SVG ชุดเดียวเต็มกล่อง ไม่ได้เอาไอคอนฟอนต์มาวางซ้อน — สัดส่วนจึงตรงกับไฟล์ `assets/img/alicecms-mark.svg` ทุกขนาด และไม่เพี้ยนถ้าฟอนต์ไอคอนโหลดไม่ทัน ตรวจแล้วว่ายังอ่านออกตั้งแต่ 16px ถึง 190px
- ประกาย AI กะพริบเบาๆ เฉพาะเมื่อผู้ใช้ตั้งระดับแอนิเมชันเป็น "เต็ม" และเครื่องไม่ได้เปิดโหมดลดการเคลื่อนไหว
- **แถบบนระบบจัดการขึ้นชื่อ AliceCMS เป็นบรรทัดหลัก** บรรทัดล่างเปลี่ยนเป็น "ระบบจัดการเว็บไซต์ของ<ชื่อหน่วยงาน>" ซึ่งต่อชื่อจากค่าตั้งค่าเว็บ ทุกหน่วยงานที่นำระบบไปใช้จะได้ชื่อของตัวเองขึ้นเอง (แทนป้ายชื่อเล็กที่เพิ่มไว้ใน 1.41.3)

---

## 1.41.3 — 2026-09-14

**เพิ่ม**
- **ป้ายชื่อ AliceCMS บนแถบบนของระบบจัดการ** วางต่อท้าย "ระบบจัดการเว็บไซต์" เป็นป้ายกลมเล็ก ชี้เมาส์ค้างจะเห็นเลขเวอร์ชันที่ติดตั้งอยู่
- ป้ายใช้สีธีมของหน่วยงาน จึงกลมกลืนไม่ว่าตั้งธีมสีอะไร และบนจอแคบกว่า 700px ป้ายจะซ่อนเองเพื่อให้ชื่อหน่วยงานมาก่อน

---

## 1.41.2 — 2026-09-14

**ปรับปรุง**
- **หน้าติดตั้งใช้ตรา AliceCMS** แทนไอคอนอาคารราชการ (`account_balance`) — หน้านี้เห็นก่อนจะมีหน่วยงานในระบบ ตราที่ถูกต้องจึงเป็นตราของตัวระบบเอง เปลี่ยนสีตามธีมที่เลือกในขั้นตอนติดตั้งด้วย
- ไอคอนเริ่มต้นของ *เว็บหน่วยงาน* ยังเป็น `account_balance` เหมือนเดิม — ตรงนั้นเป็นตราของหน่วยงานผู้ใช้ ไม่ใช่ของ CMS

---

## 1.41.1 — 2026-09-14

**ปรับปรุง**
- **แถบบนของระบบจัดการใช้ตรา AliceCMS แทนไอคอนสำเร็จรูป** — เดิมเป็นไอคอน `admin_panel_settings` จากชุดไอคอนทั่วไป ซึ่งเว็บไหนก็ใช้เหมือนกันหมด ตอนนี้เป็นตราประจำระบบ (แถบเนื้อหาสามชั้น + ประกายสามดวง)
- ตราวาดเป็น SVG ในหน้าเลยและใช้ `currentColor` จึงเปลี่ยนสีตามธีมที่หน่วยงานตั้งไว้เอง หน่วยงานที่ใช้ธีมสีเขียวหรือสีอื่นจะไม่เห็นตราน้ำเงินค้างอยู่ตัวเดียว และไม่ต้องโหลดไฟล์ภาพเพิ่ม

---

## 1.41.0 — 2026-09-14

**เพิ่ม**
- **แถบท้ายโปสเตอร์ใส่ข้อมูลหน่วยงานได้แล้ว** — เดิมมีแค่วันที่กับชื่อหน่วย ตอนนี้เพิ่มได้อีกสองบรรทัด
  - **คำขวัญ/วิสัยทัศน์หน่วยงาน** ดึงค่าเริ่มต้นจาก "วิสัยทัศน์" ในหน้าตั้งค่าเว็บให้อัตโนมัติ แก้เป็นข้อความอื่นได้
  - **แถวช่องทางติดต่อ** (เว็บไซต์ · โทรศัพท์ · อีเมล) พร้อมไอคอนกำกับ ดึงจากค่าตั้งค่าเว็บทั้งหมด ติ๊กเปิด/ปิดได้
  ภาพที่ส่งออกไปช่องทางอื่นจึงบอกได้ในตัวว่ามาจากใครและติดต่อกลับทางไหน ไม่ต้องพิมพ์ซ้ำในแคปชัน
- แถบท้ายปรับความสูงเองตามจำนวนบรรทัดที่ใส่จริง ถ้าไม่ใส่อะไรเลยก็ยุบเหลือขอบบางๆ ไม่เหลือที่ว่างค้าง
- แถวช่องทางติดต่อวัดความกว้างจริงแล้วจัดกึ่งกลางทั้งแถว ระยะห่างเท่ากันทุกช่องไม่ว่าข้อความยาวสั้นต่างกันแค่ไหน

---

## 1.40.0 — 2026-09-14

**ปรับปรุง**
- **หัวกระดาษโปสเตอร์เปลี่ยนเป็นทรงคลื่นโค้งซ้อนสองชั้น** แทนแถบสี่เหลี่ยมตัดตรง ชั้นจางอยู่ล่างโผล่เป็นชายคลื่นบางๆ ให้เกิดมิติ แบบเดียวกับหัวเอกสารประชาสัมพันธ์ของหน่วยงานราชการจริง แถบสี่เหลี่ยมเดิมดูแข็งและเหมือนเทมเพลตทั่วไป
- ตราหน่วยงานขยายใหญ่ขึ้นและวางคร่อมชายคลื่น แบบหัวจดหมายราชการ ไม่ใช่ไอคอนเล็กในกรอบ
- ชื่อหน่วยงานย่อขนาดอัตโนมัติถ้ายาวจนล้นขอบขวา จะได้ไม่ตกขอบไม่ว่าชื่อยาวแค่ไหน

---

## 1.39.1 — 2026-09-14

**ปรับปรุง**
- เพิ่ม **"ธีมเว็บนี้"** เป็นธีมสีตัวแรกในหน้าออกแบบภาพ ใช้สีธีมของเว็บหน่วยงานเอง และถูกเลือกไว้ให้ตั้งแต่เปิดหน้า — ภาพประชาสัมพันธ์ที่ใช้สีเดียวกับเว็บทำให้คนเห็นแล้วรู้ทันที ว่าเป็นของหน่วยงานไหน ไม่ใช่ภาพลอยๆ ที่ใครก็ทำได้
- ย้ายปุ่ม "สีธีมเว็บ" เดิมที่ซ่อนอยู่ข้างช่องเลือกสีออก เพราะธีมตัวแรกทำหน้าที่แทนแล้ว

---

## 1.39.0 — 2026-09-14

**เพิ่ม**
- **ลบพื้นขาวรอบตราหน่วยงานอัตโนมัติ** (เปิดไว้เป็นค่าเริ่มต้น) — ไฟล์ตราของหลายหน่วยงานเป็น JPG หรือ PNG ที่ไม่ได้ทำพื้นโปร่งมา พอวางบนหัวโปสเตอร์สีเข้มจะเห็นกรอบสี่เหลี่ยมขาวรอบตรา ระบบจะตัดพื้นขาวให้เองโดยไม่ต้องไปแก้ไฟล์ต้นฉบับ ปิดสวิตช์ได้ถ้าไม่ต้องการ

**หมายเหตุทางเทคนิค** — ใช้วิธีเติมสีจากขอบภาพเข้าไป (flood fill) ไม่ใช่ลบทุกจุดที่เป็นสีขาว เพราะตราราชการส่วนใหญ่มีสีขาวอยู่ในลวดลายด้วย ถ้าลบขาวทั้งภาพตราจะทะลุเป็นรูพรุน วิธีนี้ลบเฉพาะขาวที่ต่อเนื่องมาจากขอบ ซึ่งก็คือพื้นหลังจริงๆ แล้วไล่ความจางที่ขอบรอยตัด เพื่อไม่ให้เห็นขอบหยักเป็นขั้นบันได ระบบจะข้ามการทำงานเองถ้าภาพมีพื้นโปร่งอยู่แล้ว หรือมุมภาพไม่ได้เป็นสีขาว

---

## 1.38.1 — 2026-09-14

**ปรับปรุง**
- ตราหน่วยงานบนหัวโปสเตอร์วางบนพื้นสีตรงๆ ไม่มีแผ่นขาวรองแล้ว และขยายใหญ่ขึ้น ให้กลืนไปกับหัวกระดาษเหมือนตราบนหัวจดหมายจริง เดิมดูเหมือนสติกเกอร์แปะทับ
- เพิ่มสวิตช์ **รองพื้นขาวหลังตรา** (ปิดไว้เป็นค่าเริ่มต้น) เผื่อหน่วยงานที่ตราเป็นสีเข้ม แล้วจมหายไปบนพื้นสีเข้ม

---

## 1.38.0 — 2026-09-14

**เพิ่ม**
- **เทมเพลต "โปสเตอร์ข่าว"** สำหรับสื่อประชาสัมพันธ์แนวตั้งโดยเฉพาะ วางแบบเอกสารราชการ — หัวกระดาษ (ตราหน่วยงาน + ชื่อหน่วยงาน + สังกัด) → รูปข่าวขนาดใหญ่ → ป้ายประเภทและเนื้อหาบนพื้นขาว → แถบท้ายที่มีวันที่และเว็บไซต์ หัวกระดาษคือสิ่งที่ทำให้ดูเป็นสื่อทางการของหน่วยงาน ไม่ใช่ภาพที่ใครก็ทำได้ และตั้งเป็นรูปแบบเริ่มต้นแล้ว
- **ขนาดโปสเตอร์แนวตั้ง 1080×1620** (สัดส่วน 2:3 แบบโปสเตอร์จริง) เป็นขนาดเริ่มต้น
- ช่องกรอก **ชื่อหน่วยงาน / สังกัด / วันที่** แยกจากข้อความมุมล่าง ดึงค่าจากการตั้งค่าเว็บและวันที่เผยแพร่ข่าวมาให้อัตโนมัติ

---

## 1.37.0 — 2026-09-14

**เพิ่ม**
- **ปุ่ม "สร้างภาพ" ในหน้าจัดการข่าว** — กดแล้วระบบดึงหัวข้อ เนื้อหาย่อ และรูปปกของข่าวนั้น มาใส่ในหน้าออกแบบให้อัตโนมัติ พร้อมสลับไปเทมเพลตที่ใช้รูปให้เลย ปรับนิดหน่อยแล้วดาวน์โหลดไปโพสต์ Facebook / Line ได้ทันที ไม่ต้องพิมพ์ซ้ำ
- **ธีมสีสำเร็จรูป 8 ชุด** — ทางการ, ราชการ, น้ำเงินสด, เขียวมรกต, ม่วงหรูหรา, ส้มอบอุ่น, เทาสุขุม, ชมพูเข้ม ทุกชุดคัดเฉดที่เข้มพอให้ตัวอักษรอ่านออกบนพื้น และยังเลือกสีเองได้เหมือนเดิม

---

## 1.36.2 — 2026-09-14

**ปรับปรุง**
- หัวหน้าโครงสร้างผู้บริหารจัดชิดซ้ายให้ตรงกับหน้าอื่น เดิมถูกบังคับให้อยู่กึ่งกลางอยู่หน้าเดียว

---

## 1.36.1 — 2026-09-14

**แก้ไข (สำคัญ)**
- **ปิด section แล้วหน้ายังเข้าได้อยู่** — สวิตช์ปิด section เดิมซ่อนออกจากหน้าแรก เมนู และแผนผังเว็บเท่านั้น แต่ตัวไฟล์หน้ายังเปิดได้ตามปกติ ใครมีลิงก์ตรงก็ยังเข้าได้ ที่หนักที่สุดคือ **หน้าร้องเรียนยังรับเรื่องจากประชาชนเข้าฐานข้อมูลต่อไปทั้งที่ผู้ดูแลปิดไปแล้ว** (พบจากหน้างานจริง: ปิดแล้วแต่ยังมีเรื่องส่งเข้ามา เพราะ Google เก็บหน้าไว้ตั้งแต่ตอนที่ยังเปิดอยู่)
- เพิ่มตัวกัน `require_section_live()` ในทุกหน้าที่ผูกกับ section ได้แก่ ร้องเรียน, ติดตามเรื่องร้องเรียน, เอกสารเผยแพร่, ITA/OIT, จัดซื้อจัดจ้าง, คำถามที่พบบ่อย, วิดีโอ (ทั้งหน้ารวมและหน้าเล่นเดี่ยว) และผู้บริหาร
- ตอบ 404 แทนข้อความ "ปิดปรับปรุง" ที่สถานะ 200 เพราะถ้าตอบ 200 Google จะเก็บหน้าไว้ในดัชนีต่อ แล้วคนก็จะคลิกเข้ามาเรื่อยๆ

หน้าจะถูกบล็อกก็ต่อเมื่อปิดครบทั้งสองสวิตช์ (แสดงบนหน้าแรก และ แสดงในเมนู) ถ้ายังเปิดไว้ทางใดทางหนึ่งหน้ายังเข้าได้ตามปกติ

---

## 1.36.0 — 2026-09-14

**เพิ่ม**
- **ใส่รูปภาพในภาพประกาศได้** — อัปโหลดจากเครื่อง หรือคลิกเลือกจากคลังสื่อที่แสดงอยู่ในหน้าเดียวกัน พร้อมแถบปรับความเข้มฉากมืดทับรูป เพื่อให้ตัวอักษรอ่านออกบนรูปทุกแบบ
- **เพิ่มเป็น 4 รูปแบบ** — การ์ดประกาศ, ข้อความเน้น, รูปเต็มพื้น (ข้อความทับรูปพร้อมไล่เงามืดจากล่าง) และรูปครึ่งบน (รูปครึ่งบน ข้อความบนพื้นสีครึ่งล่าง)
- **ปรับขนาดตัวอักษรได้ 70–135%** และเลือกตำแหน่งข้อความแนวตั้งได้ (ชิดบน / กึ่งกลาง / ชิดล่าง) สำหรับกรณีที่การจัดอัตโนมัติไม่ได้ดั่งใจ
- เลือกไอคอนได้ 6 แบบ และทุกเทมเพลตดึงตราหน่วยงานกับสีธีมมาใช้อัตโนมัติ

**ปรับปรุง**
- ภาพที่มีรูปถ่ายบันทึกเป็น JPEG แทน PNG — PNG ของภาพถ่ายใหญ่กว่าหลายเท่าและชนเพดานอัปโหลดง่าย ส่วนภาพพื้นสีเรียบยังใช้ PNG เพราะตัวอักษรคมกว่าและไฟล์เล็กอยู่แล้ว ฝั่งเซิร์ฟเวอร์ตรวจลายเซ็นไฟล์จากเนื้อไฟล์จริงทั้งสองรูปแบบ

---

## 1.35.0 — 2026-09-14

**เพิ่ม**
- **ออกแบบภาพประกาศในตัว** (เมนู เครื่องมือสร้างเอง → ออกแบบภาพประกาศ) — กรอกข้อความแล้วเห็นผลทันที บันทึกเข้าคลังสื่อเพื่อใช้เป็นภาพปกข่าวได้เลย ไม่ต้องออกไปทำในแอปภายนอก
- 2 รูปแบบ: การ์ดประกาศ (มีกรอบขาว เหมาะกับข้อความยาว) และข้อความเน้น (ตัวใหญ่เต็มพื้น เหมาะกับประกาศสั้น)
- 3 ขนาด: จัตุรัส 1080×1080, แนวตั้ง 1080×1350 และแนวนอน 1200×630 สำหรับภาพปกลิงก์
- ดึงตราหน่วยงานและสีธีมจากการตั้งค่าเว็บมาใช้อัตโนมัติ เลือกไอคอนได้ 5 แบบ

**หมายเหตุทางเทคนิค** — วาดด้วย Canvas API ของเบราว์เซอร์ ไม่เพิ่มไลบรารีภายนอกแม้แต่ตัวเดียว เหตุผลคือเบราว์เซอร์จัดรูปอักษรไทย (สระบน-ล่าง วรรณยุกต์) ได้ถูกต้องอยู่แล้ว ขณะที่การวาดฝั่งเซิร์ฟเวอร์ด้วย GD ต้องจัดการเองและมักเพี้ยน และตัดบรรทัดภาษาไทยด้วย Intl.Segmenter ซึ่งใช้พจนานุกรมในตัวเบราว์เซอร์ (ภาษาไทยไม่เว้นวรรคระหว่างคำ จะตัดตามช่องว่างแบบภาษาอังกฤษไม่ได้)

---

## 1.34.0 — 2026-09-13

**เพิ่ม**
- โลโก้ประจำ AliceCMS (`assets/img/alicecms-mark.svg`) — ชั้นเนื้อหาสามแถบพร้อมประกายสามดวง สื่อถึงการจัดการเนื้อหาและการช่วยเขียนด้วย AI ใช้สีน้ำเงิน #1A73E8 ชุดเดียวกับตระกูล Alice
- แสดงโลโก้ที่ท้ายฝั่งซ้ายของหน้ายืนยันตัวตน และที่หัว README

**หมายเหตุ** — ไม่ได้แตะ `favicon.svg` และไอคอน PWA ของระบบ เพราะสองอย่างนั้นเป็นไอคอนของ *เว็บหน่วยงาน* ที่ประชาชนเห็นบนแท็บและหน้าจอมือถือ ไม่ควรเป็นโลโก้ของ CMS

---

## 1.33.1 — 2026-09-13

**ปรับปรุง**
- เอาตราหน่วยงานที่เป็นลายน้ำด้านหลังฝั่งซ้ายออก ตราของแต่ละหน่วยงานรูปทรงต่างกันมาก บางอันมีด้ามหรือส่วนแหลมยื่นออกมา พอขยายใหญ่แล้วดูรกมากกว่าช่วยให้สวยขึ้น

---

## 1.33.0 — 2026-09-13

**ปรับปรุง**
- ออกแบบหน้ายืนยันตัวตนใหม่ให้ใช้ภาษาออกแบบชุดเดียวกับ alicentia.me ซึ่งเป็นผลิตภัณฑ์ตระกูลเดียวกัน — พื้นขาวอมฟ้ากับแสงฟุ้งน้ำเงินสองจุด การ์ดขาวมุมมนขอบฟ้าจาง เงาอมน้ำเงินไม่ใช่เงาเทา และปุ่มทรงแคปซูล โปร่งและสว่างแทนแผ่นสีทึบเต็มครึ่งจอแบบเดิม
- สีหลักกลับมาเป็นน้ำเงิน #1A73E8 ตรงกับ Alicentia (รุ่นก่อนหน้าเลือกเขียวมิ้นต์จากการ์ดโปรไฟล์ใบเดียว ซึ่งไม่ใช่สีประจำแบรนด์)
- ตราหน่วยงานวางเป็นพื้นผิวจางๆ ด้านหลังฝั่งซ้าย ผูกหน้าเข้ากับหน่วยงานโดยไม่รบกวนความโปร่ง

---

## 1.32.2 — 2026-09-13

**ปรับปรุง**
- เปลี่ยนโทนสีหน้ายืนยันตัวตนเป็นเขียวมิ้นต์ ซึ่งเป็นสีประจำตระกูลผลิตภัณฑ์ Alice (Alicentia, Alicetools) ให้ผลงานชุดเดียวกันดูเป็นพวกเดียวกัน
- เลือกเฉดที่ลึกกว่าสีพาสเทลต้นแบบ เพื่อให้ตัวอักษรขาวบนแผงอ่านออกตลอดทั้งแผง

---

## 1.32.1 — 2026-09-13

**ปรับปรุง**
- ปรับหน้ายืนยันตัวตนให้มีน้ำหนักขึ้น — นำตราของหน่วยงานมาทำเป็นลายน้ำขนาดใหญ่จางๆ บนแผงซ้ายแบบตราดุนนูนบนเอกสารราชการ แทนที่จะย่อใส่กล่องเล็กอย่างเดียว
- ไล่เฉดพื้นหลังคมขึ้น (ฟ้าสด → น้ำเงินกรม) ของเดิมไล่จางจนอ่านเป็นแผ่นสีเดียว ดูแบน
- ตัดรายการคุณสมบัติ 3 ข้อบนแผงซ้ายออก เป็นคำโฆษณาที่ไม่ได้ช่วยคนกำลังจะล็อกอิน
- ตราหน่วยงานใช้กรอบสี่เหลี่ยมมุมมนแทนวงกลม เพราะตราที่มีด้ามหรือทรงสูงจะถูกวงกลมตัดขอบ

---

## 1.32.0 — 2026-09-13

**ปรับปรุง**
- ออกแบบหน้าเข้าสู่ระบบ ลืมรหัสผ่าน และตั้งรหัสผ่านใหม่ ใหม่ทั้งหมด — แบ่งเป็นสองฝั่ง ฝั่งซ้ายแสดงตัวตนของหน่วยงาน (โลโก้จริงที่อัปโหลดไว้ ชื่อ สังกัด) ฝั่งขวาเป็นฟอร์ม จอเล็กกว่า 920px ยุบเหลือคอลัมน์เดียวพร้อมหัวแบรนด์ย่อ
- หน้ายืนยันตัวตนใช้โทนน้ำเงิน Google Material เป็นเอกลักษณ์ของระบบเอง ไม่ตามธีมสีของหน่วยงาน (แนวเดียวกับที่ WordPress ใช้โทนของตัวเองไม่ตามธีมเว็บ) เพราะธีมสีจัดๆ จะกลายเป็นแผงสีทึบ เต็มครึ่งจอซึ่งกลืนกับฟอร์มได้ยาก — เปลี่ยนกลับไปตามธีมได้โดยลบตัวแปรสองบรรทัดใน .auth-shell
- เพิ่มปุ่มแสดง/ซ่อนรหัสผ่าน ลดโอกาสพิมพ์ผิดจนถูกล็อก 15 นาทีโดยไม่รู้ตัว
- ช่องกรอกมีเส้นขอบและวงแหวนโฟกัสชัดขึ้น

**แก้ไข**
- สีพื้นหลังโลโก้ถูกฝังเป็นน้ำเงินไว้ตายตัว หน่วยงานที่ตั้งธีมสีอื่นจึงเห็นโลโก้สีน้ำเงินคู่กับปุ่มสีธีม ซึ่งดูเหมือนทำไม่เสร็จ แก้ให้ตามสีธีมแล้ว (มีผลกับหน่วยงานที่ยังไม่ได้อัปโหลดโลโก้ของตัวเอง)

---

## 1.31.1 — 2026-09-12

**แก้ไข**
- ปิดการเข้าถึง `README.md`, `CHANGELOG.md`, `SECURITY.md`, `LICENSE` และไฟล์ `.md` อื่นจากเว็บ ไฟล์เหล่านี้ต้องมากับแพ็กเกจ (สัญญาอนุญาต MIT กำหนดให้แนบ LICENSE ไปกับทุกสำเนา) แต่ไม่ควรเปิดอ่านได้จากเว็บจริง เพราะบอกเวอร์ชันที่รันอยู่และรายการช่องโหว่ที่เพิ่งแก้

---

## 1.31.0 — 2026-09-12

รุ่นแรกที่เผยแพร่เป็นโอเพนซอร์ส

**เปลี่ยนแปลง**
- ย้ายช่องทางแจกจ่ายมาที่ GitHub Releases — ดาวน์โหลดผ่าน HTTPS แทน HTTP บน IP เปล่า และไม่ผูกระบบอัปเดตของทุกเว็บไว้กับเซิร์ฟเวอร์ส่วนตัวเครื่องเดียวอีกต่อไป
- เปลี่ยนชื่อไฟล์แพ็กเกจจาก `govcms-x.x.x.zip` เป็น `alicecms-x.x.x.zip`
- `UPDATE_MANIFEST_URL` เริ่มต้นเป็นค่าว่าง — ระบบจะไม่เชื่อมต่อออกไปไหนจนกว่าผู้ดูแลจะกรอก URL เอง เจ้าของเว็บควรเป็นผู้เลือกเองว่าจะรับอัปเดตจากใคร
- เพิ่ม `LICENSE` (MIT), `SECURITY.md`, `CHANGELOG.md` และเขียน `README.md` ใหม่

**หมายเหตุสำหรับผู้ที่ใช้อยู่เดิม** — หลังอัปเดตรุ่นนี้ ต้องตั้ง URL เซิร์ฟเวอร์อัปเดตหนึ่งครั้งที่ **ตั้งค่า → อัปเดตระบบ** (ค่าที่ตั้งจะถูกเก็บในฐานข้อมูล การอัปเดตครั้งต่อไปจะไม่ลบทิ้ง) หรือกำหนดใน `config.php` ซึ่งระบบอัปเดตไม่เขียนทับ

---

## 1.30.2 — 2026-09-12

**แก้ไข**
- อัปเดตที่ล้มเหลวจะไม่ทิ้งชุดสำรองค้างไว้อีก เดิมเมื่ออัปเดตไม่สำเร็จ ระบบลบเฉพาะโฟลเดอร์ชั่วคราวแต่เก็บชุดสำรอง (ฐานข้อมูล + ไฟล์ระบบ) ไว้ ทุกครั้งที่ผู้ดูแลกดลองใหม่จึงกินพื้นที่เพิ่มเรื่อยๆ จนดิสก์เต็ม แล้วลามไปทำให้เขียนไฟล์เซสชันไม่ได้ = ล็อกอินหลังบ้านไม่ได้
- เก็บกวาดชุดสำรองเก่า **ก่อน** เริ่มสำรองชุดใหม่ เดิมทำหลังอัปเดตสำเร็จเท่านั้น ซึ่งช่วยอะไรไม่ได้เมื่อพื้นที่ใกล้เต็มเพราะจะล้มระหว่างทางก่อนเสมอ
- ข้อความเมื่อแตกไฟล์ไม่สำเร็จบอกสาเหตุที่แท้จริง (พื้นที่ดิสก์) แทนที่จะปล่อยให้เข้าใจผิดว่าไฟล์แพ็กเกจเสียหาย

ชุดสำรองยังถูกเก็บไว้ตามเดิมเมื่ออัปเดตล้มเหลว **หลัง** เริ่มติดตั้งไฟล์แล้ว เพื่อให้กู้คืนได้

## 1.30.1 — 2026-09-12

**ปรับปรุง**
- การ์ดวิดีโอใช้สไตล์ชุดเดียวกับการ์ดข่าว — ความสูงปก ระยะขอบ เงา และมุมโค้งตรงกัน เดิมปกวิดีโอใช้สัดส่วน 16:9 ทำให้การ์ดสูงกว่าส่วนอื่นของเว็บเกือบ 80px
- เพิ่มป้าย "ใหม่" บนวิดีโอที่เผยแพร่ภายใน 7 วัน
- บรรทัดวันที่ชิดขอบล่างเสมอ การ์ดในแถวเดียวกันจึงมีขอบล่างตรงแนวกันแม้คำอธิบายยาวไม่เท่ากัน

## 1.30.0 — 2026-09-04

**เพิ่ม**
- แผนผังเว็บ (`sitemap.php`) ครอบคลุมหน้าวิดีโอ เนื้อหาแบบกำหนดเอง และแบบฟอร์ม ซึ่งเดิมตกหล่นทั้งหมด
- ข้อมูลโครงสร้าง (JSON-LD) ระบุประเทศ ช่องทางติดต่อสำหรับประชาชน และประกาศว่าเว็บมีระบบค้นหาในตัว

## 1.29.1 — 2026-09-04

**แก้ไข**
- ลบชุดสำรองเก่าอัตโนมัติหลังอัปเดตสำเร็จ (เก็บ 3 ชุดล่าสุด) เดิมฟังก์ชันนี้ถูกเรียกจากสคริปต์ cron เท่านั้น ซึ่งโฮสต์ทั่วไปไม่ได้ตั้งไว้ ทำให้ชุดสำรองสะสมจนพื้นที่เต็ม

## 1.29.0 — 2026-09-02

**แก้ไข**
- ล็อกอินหลังบ้านไม่ได้ ขึ้นว่า "การตรวจสอบความปลอดภัยล้มเหลว (CSRF)" — ต้นเหตุคือที่เก็บไฟล์เซสชันของเซิร์ฟเวอร์เขียนไม่ได้ ทำให้ข้อมูลเซสชันหายทุกคำขอ รหัส CSRF ในฟอร์มจึงไม่มีวันตรงกับฝั่งเซิร์ฟเวอร์ อาการนี้กระทบฟอร์มสาธารณะทั้งหมดด้วย (ร้องเรียน/แบบฟอร์มออนไลน์)
- ย้ายที่เก็บเซสชันมาไว้ที่ `storage/sessions` ของเว็บเอง พร้อมถอยกลับไปใช้ค่าของเซิร์ฟเวอร์อัตโนมัติหากเขียนไม่ได้

---

## 1.0.0 – 1.28.2 (2026-06-12 – 2026-09-01)

73 เวอร์ชันแรกอยู่ในช่วงพัฒนาก่อนเปิดซอร์ส ยังไม่ได้บันทึกรายการเปลี่ยนแปลงไว้เป็นไฟล์

ความสามารถหลักที่เพิ่มเข้ามาในช่วงนี้ ได้แก่ ระบบข่าวและเอกสารเผยแพร่, ITA/OIT, จัดซื้อจัดจ้าง,
ร้องเรียนออนไลน์พร้อมติดตามสถานะ, แบบฟอร์มที่สร้างเองได้, เนื้อหาแบบกำหนดเอง, วิดีโอ,
โครงสร้างองค์กรและทำเนียบบุคลากร, ถังขยะกู้คืนได้, สำรองข้อมูลอัตโนมัติ,
ยืนยันตัวตนสองชั้น (TOTP), PWA, URL แบบซ่อน `.php` และระบบอัปเดตผ่านหน้าจัดการที่ตรวจลายเซ็นดิจิทัล
