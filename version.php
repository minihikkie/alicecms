<?php
/**
 * version.php — เวอร์ชันของระบบ CMS
 * ไฟล์นี้จะถูกเขียนทับอัตโนมัติเมื่ออัปเดตผ่านหน้า admin — ไม่ต้องแก้ด้วยมือ
 */
define('APP_VERSION', '1.45.0');
define('APP_BUILD', '2026-09-15');

/* URL ของเซิร์ฟเวอร์อัปเดต — ตั้งค่าได้ที่หน้า ตั้งค่า → อัปเดตระบบ
 *
 * ค่าเริ่มต้นเว้นว่างไว้โดยตั้งใจ: ระบบจะไม่เชื่อมต่อออกไปไหนเลยจนกว่าผู้ดูแลจะกรอก URL เอง
 * ถ้าใส่ค่าเริ่มต้นเป็นเซิร์ฟเวอร์ของผู้พัฒนา ทุกเว็บที่ติดตั้งระบบนี้จะวิ่งไปเช็คอัปเดตที่นั่น
 * และรับโค้ดจากที่นั่นมาติดตั้งทับตัวเอง — เจ้าของเว็บควรเป็นคนเลือกเองว่าจะเชื่อใคร
 *
 * ถ้าจะตั้งเซิร์ฟเวอร์อัปเดตของตัวเอง:
 *   1) สร้างคู่กุญแจใหม่ แล้ววาง public key ใน includes/updater.php
 *      openssl genrsa -out private.pem 2048
 *      openssl rsa -in private.pem -pubout -out public.pem
 *   2) build แพ็กเกจด้วย tools/build-release.php (เซ็นด้วย private key ของคุณ)
 *   3) อัปโหลด .zip กับ update.json ขึ้นเว็บเซิร์ฟเวอร์ใดก็ได้ แล้วกรอก URL ที่หน้า admin
 *
 * แนะนำพอร์ต 80/443 — บางเครือข่าย โดยเฉพาะหน่วยงานราชการ ปิดกั้นพอร์ตที่ไม่ใช่มาตรฐาน
 * ทำให้ตรวจสอบอัปเดตไม่ได้เลย
 */
if (!defined('UPDATE_MANIFEST_URL')) {
    define('UPDATE_MANIFEST_URL', '');
}
