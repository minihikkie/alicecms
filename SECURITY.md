# นโยบายความปลอดภัย

## แจ้งช่องโหว่

ระบบนี้ถูกนำไปใช้กับเว็บไซต์หน่วยงานราชการที่ให้บริการประชาชนจริง
ช่องโหว่ที่ถูกเปิดเผยสาธารณะก่อนมีแพตช์ จึงกระทบเว็บที่ใช้งานอยู่ทันที

**กรุณาอย่าเปิด public issue สำหรับช่องโหว่ด้านความปลอดภัย**

ช่องทางแจ้งแบบส่วนตัว:

1. **GitHub Security Advisory** (แนะนำ) — แท็บ **Security** → **Report a vulnerability**
2. อีเมล: `<ใส่อีเมลสำหรับรับแจ้งช่องโหว่>`

สิ่งที่ช่วยให้แก้ได้เร็วขึ้น:

- ขั้นตอนที่ทำให้เกิดซ้ำได้
- เวอร์ชันของระบบ (ดูที่ `version.php` หรือมุมล่างหน้า admin)
- เวอร์ชัน PHP และเว็บเซิร์ฟเวอร์
- ผลกระทบที่ประเมินได้ เช่น อ่านข้อมูลผู้อื่นได้ / ยกระดับสิทธิ์ / รันโค้ดได้

## การตอบกลับ

| ขั้น | กรอบเวลาที่ตั้งใจไว้ |
|---|---|
| ตอบรับว่าได้รับเรื่องแล้ว | ภายใน 7 วัน |
| ประเมินและยืนยันช่องโหว่ | ภายใน 30 วัน |
| ออกแพตช์ | ตามความรุนแรง — ระดับวิกฤตจะเร่งที่สุด |

โปรเจกต์นี้ดูแลโดยผู้พัฒนารายเดียวในเวลาว่าง กรอบเวลาข้างต้นเป็นความตั้งใจ ไม่ใช่ข้อผูกพันตามสัญญา

## เวอร์ชันที่ยังได้รับแพตช์

แพตช์ความปลอดภัยออกให้เฉพาะ **เวอร์ชันล่าสุด** เท่านั้น
ถ้าใช้เวอร์ชันเก่าอยู่ กรุณาอัปเดตก่อนแจ้งปัญหา

## ขอบเขต

**อยู่ในขอบเขต** — โค้ดในรีโปนี้ทั้งหมด

**ไม่อยู่ในขอบเขต**
- การตั้งค่าเซิร์ฟเวอร์ผิดพลาดของผู้ติดตั้งเอง (เช่น เปิดสิทธิ์ไฟล์กว้างเกินไป, ไม่ใช้ HTTPS)
- ช่องโหว่ของ PHP, MySQL หรือเว็บเซิร์ฟเวอร์เอง
- การโจมตีที่ต้องมีสิทธิ์ผู้ดูแลอยู่แล้ว
- รายงานจากเครื่องมือสแกนอัตโนมัติที่ไม่ได้พิสูจน์ว่าใช้โจมตีได้จริง

## สำหรับผู้ที่นำระบบไปใช้

ถ้าคุณ fork หรือตั้งเซิร์ฟเวอร์อัปเดตของตัวเอง:

- **สร้างคู่กุญแจของคุณเอง** อย่าใช้กุญแจของโปรเจกต์ต้นทาง
- **เก็บ `private.pem` ไว้นอกโฟลเดอร์โปรเจกต์** — ถ้าหลุด ผู้อื่นจะปลอมแพ็กเกจอัปเดตให้ทุกเว็บที่ใช้กุญแจคู่นั้นยอมรับได้
- ตรวจว่าไฟล์ `config.php` และโฟลเดอร์ `storage/` เปิดจาก URL ตรงไม่ได้
- เปิด HTTPS และยืนยันตัวตนสองชั้นในระบบจัดการ
