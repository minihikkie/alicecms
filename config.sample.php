<?php
/**
 * config.sample.php — ตัวอย่างไฟล์ตั้งค่า
 * ปกติไฟล์ config.php จะถูกสร้างอัตโนมัติโดย install.php
 * ใช้ไฟล์นี้เฉพาะกรณีต้องสร้าง config.php เอง (เช่นย้าย server):
 * คัดลอกเป็น config.php แล้วแก้ค่าให้ตรงกับเครื่องจริง
 */
define('DB_HOST', 'localhost');     // โฮสต์ฐานข้อมูล
define('DB_NAME', 'gov_website');   // ชื่อฐานข้อมูล
define('DB_USER', 'root');          // ผู้ใช้ฐานข้อมูล
define('DB_PASS', '');              // รหัสผ่านฐานข้อมูล
define('BASE_URL', '');             // path ของเว็บ เช่น '' (รากโดเมน) หรือ '/mysite' (โฟลเดอร์ย่อย)

/* ── ไม่บังคับ ─────────────────────────────────────────────────────────
 * เซิร์ฟเวอร์อัปเดตของคุณเอง — ตั้งที่นี่จะทับค่าใน version.php
 * เหมาะกับกรณีที่ดูแลหลายเว็บ เพราะไฟล์นี้ระบบอัปเดตไม่เขียนทับ
 * (ตั้งผ่านหน้า ตั้งค่า → อัปเดตระบบ ก็ได้เหมือนกัน)
 *
 * ปล่อยไว้แบบนี้ = ไม่เชื่อมต่อออกไปไหนเลย ต้องอัปเดตด้วยการอัปโหลดไฟล์เอง
 */
// define('UPDATE_MANIFEST_URL', 'https://update.example.org/update.json');
